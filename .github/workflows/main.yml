name: Deploy on Tag

on:
  push:
    tags:
      - 'v*'  # Run the workflow when a tag starting with 'v' is pushed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Deploy
        run: |
          # Add your deployment steps here
          # You can use the $GITHUB_REF environment variable to access the tag name
          echo "Deploying tag: $GITHUB_REF"
